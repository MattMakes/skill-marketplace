<!-- Child CORE doc.

     Everything in this template that is guidance rather than content lives in
     an HTML comment on purpose. Claude Code strips block-level HTML comments
     before a CLAUDE.md reaches the context window, but the Read tool still
     shows them -- so this guidance is fully visible to whoever writes the doc
     and costs nothing on every later load. That matters here more than in most
     files, because this one loads automatically every time an agent touches
     this directory. Prose left unfilled is not a neutral placeholder, it is a
     bill charged on every visit.

     So: fill a section with something specific, or delete the heading. An
     empty section is worse than a missing one. Aim for under ~60 lines total.
-->

# {{DIR}}

<!-- CORE:begin -->

## Purpose

{{PURPOSE}}

<!-- One short paragraph: what this directory is for, in terms someone who has
     never opened it would understand. The parent's Child CORE Index quotes
     this, so make the first sentence stand on its own. -->

## Ownership

<!-- What lives here, and what deliberately does not. Name the neighbouring
     directory that owns anything a reader might reasonably expect to find here
     and won't -- that pointer saves more searching than a list of what is
     present, which they can see for themselves. -->

## Local Contracts

<!-- What callers outside this directory may depend on, and what is free to
     change without warning. Invariants that must hold.

     Only rules that differ from the rest of the repository belong here; a
     repo-wide rule stated again in a child doc is one more place for the two to
     drift apart.

     Before writing "must not depend on X", "nothing else uses Y" or "is only
     reached from Z", grep for it. The knowledge graph records the edges it
     resolved and cannot prove absence, so a negative claim taken from a graph
     query is exactly the kind that turns out to be false and then gets believed
     by every session afterwards. -->

## Work Guidance

<!-- Conventions for changing things here: patterns to follow, traps that have
     caught people before, the reason behind a choice that looks wrong at first
     glance. Delete this heading until you have something specific. -->

## Verification

<!-- The exact command that proves a change here works. Delete this heading if
     no such command exists yet, and add it back when one does. -->

## Child CORE Index

<!-- Regenerated by `python3 .claude/core/core.py index`. Edit the child docs,
     not this list. -->

<!-- CORE:end -->
