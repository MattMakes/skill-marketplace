# Bounded Context Canvas — <Context name>

<!-- generated by ddd define render from ddd/07-define/define.json on <ISO date>. Edit define.json and re-render; delete this line to protect hand edits. -->

| | |
|---|---|
| **Context id** | `<context-id>` |
| **Owning team** | <Team name> (`<team-id>`, <stream-aligned/platform/enabling/complicated-subsystem>) |
| **Deployable** | <Deployable name> (`<deployable-id>`, <modular-monolith/service/…>, data store: <own/shared>; shared with `<other-context>`) |
| **Subdomains** | `<subdomain-id>`, … |
| **Implementation pattern** | <transaction-script/active-record/domain-model/event-sourced-domain-model> (sourcing: <build/buy/…>, investment: <high/medium/low>) |
| **Aggregates (candidates)** | `<aggregate-id>`, … — or "— (not a domain model)" |

## Purpose

<2–4 sentences, business language: what this context decides/provides → for whom → so that (goal id) → what it does NOT own (which neighbour does). No technology.>

## Strategic classification

| Domain | Business model | Evolution |
|---|---|---|
| core / supporting / generic | revenue-generator / engagement-creator / compliance-enforcer / cost-reducer | genesis / custom / product / commodity |

_Strategize rationale_: <copied from strategize.classifications[].rationale> — _future_: <future_direction>

## Domain roles

**<role-id>**, **<role-id>** — <one or two sentences of justification from the upstream facts (aggregates, who commands it, external systems, policies)>

## Inbound communication

| Collaborator | Type | Relationship | Message | Kind | Via | Payload |
|---|---|---|---|---|---|---|
| <Name> (`<id>`) | person / bounded context / external system | user-interaction / customer-supplier / … | `<message-id>` <Message Name> | command / event / query | http, sync / in-process, async, at-least-once | <payload fields>; response: `<id>` (<fields>) — a response is never its own row |

_Read models that inform its commands_: **<Read model>** (`<id>`, informs `<command>`, used by <actor>); **<canvas-added read model>** (informs `<command>`, <note>)

## Ubiquitous language

| Term | Meaning in this context | Avoid |
|---|---|---|
| **<Term>** | <one sentence: what it IS here; note if it differs from a neighbour's meaning> | <synonyms to avoid> |

## Business decisions

1. <Rule as a decision: condition → outcome, testable, domain words, thresholds with numbers (assumption id if guessed)>
2. <…>

## Outbound communication

| Message | Kind | Via | Payload | Collaborator | Type | Relationship |
|---|---|---|---|---|---|---|
| `<message-id>` <Message Name> | event / command / query | in-process, async, at-least-once | <payload fields> | <Name> (`<id>`) | bounded context / external system | published-language / conformist / … |

_Also emits (no consumer known yet)_: `<event-id>`, …   ← owned events absent from the rows above

## Assumptions

- [<step> A<n>] <carried upstream assumption>
- <this step's own assumption: heuristic used, guessed threshold, chosen relationship label>

## Verification metrics

- <business metric tied to a goal: name, target, horizon>
- <structural metric: does the boundary hold? e.g. "≥ 80 % of changes touch only this module">

## Open questions

- [<step> Q<n> / H<n> / CC<n>] <carried question, hotspot or coupling concern>
- <new question you would have asked a domain expert>

## Quality attributes

| Attribute | Priority | Requirement | Scenario |
|---|---|---|---|
| <consistency / latency / availability / …> | high / medium / low | <measurable requirement> | <when (stimulus, environment) → then (response, measure)> |

## Model traits and design notes   ← only at depth `deep` or when `model_traits` is set

- <e.g. "draft semantics until meals-chosen; signed semantics after — no edits, only a new week">

---
_Layout: Bounded Context Canvas v5 (ddd-crew). Sources: `ddd/03-decompose`, `04-strategize`, `05-connect`, `06-organise`, `02-discover`. Downstream: `ddd-code` reads this canvas and `define.json`._
